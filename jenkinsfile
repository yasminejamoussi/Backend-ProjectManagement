pipeline {
    agent {
        docker {
            image 'node:18-bullseye'
            args '-u root'
        }
    }

    environment {
        BACKEND_REPO = 'https://github.com/yasminejamoussi/Backend-ProjectManagement.git'
        FRONTEND_REPO = 'https://github.com/yasminejamoussi/FrontEnd-ProjectManagement.git'
        SONAR_TOKEN = credentials('sonarqube')
        NEXUS_URL = '192.168.33.10:8083'
        NEXUS_REPOSITORY = 'dockr-repo'
        NEXUS_CREDENTIAL_ID = 'nexus'
    }

    stages {
        stage('Checkout Code') {
            steps {
                script {
                    echo "📥 Clonage du Backend..."
                    sh "git clone -b PipelinePi ${BACKEND_REPO} backend"
                    echo "📥 Clonage du Frontend..."
                    sh "git clone ${FRONTEND_REPO} frontend"
                }
            }
        }

        stage('Install Dependencies') {
            parallel {
                stage('Backend') {
                    steps {
                        dir('backend') {
                            echo "📦 Installation des dépendances du backend..."
                            sh 'apt-get update && apt-get install -y build-essential python3'
                            sh 'npm cache clean --force'
                            sh 'rm -rf node_modules package-lock.json'
                            sh 'npm install'
                            sh 'npm install --save-dev jest-junit'
                        }
                    }
                }
                stage('Frontend') {
                    steps {
                        dir('frontend') {
                            echo "📦 Installation des dépendances du frontend..."
                            sh 'npm cache clean --force'
                            sh 'rm -rf node_modules package-lock.json'
                            sh 'npm install'
                        }
                    }
                }
            }
        }

        stage('Start MongoDB and Application') {
            steps {
                dir('backend') {
                    echo "🚀 Démarrage de MongoDB et de l'application..."
                    sh 'docker-compose -f docker-compose.yaml up -d'
                    // Attendre que MongoDB soit prêt
                    sh 'docker-compose -f docker-compose.yaml exec -T mongotest mongo --eval "printjson(db.adminCommand({ ping: Ascending))" || sleep 5 && docker-compose -f docker-compose.yaml exec -T mongotest mongo --eval "printjson(db.adminCommand({ ping: 1 }))"'
                }
            }
        }

        stage('Unit Test Backend') {
            steps {
                dir('backend') {
                    echo "🧪 Exécution des tests..."
                    timeout(time: 10, unit: 'MINUTES') {
                        sh 'docker-compose -f docker-compose.yaml run --rm api npx jest test/projectManagement.test.js --ci --testTimeout=40000 --reporters=default --reporters=jest-junit'
                    }
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    def scannerHome = tool 'sonarqube'
                    withSonarQubeEnv('sonarqube') {
                        dir('backend') {
                            echo "🚀 Analyse SonarQube..."
                            timeout(time: 10, unit: 'MINUTES') {
                                withEnv(["SONAR_LOGIN=${SONAR_TOKEN}"]) {
                                    sh """
                                    ${scannerHome}/bin/sonar-scanner \
                                     -Dsonar.projectKey=ProjectManagementPI \
                                    -Dsonar.projectName=ProjectManagementPI \
                                    -Dsonar.sources=src \
                                    -Dsonar.language=js \
                                    -Dsonar.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/**,**/coverage/**,src/utils/**,src/controllers/**,src/routes/**,src/scripts/**,src/middleware/**,src/server.js \
                                    -Dsonar.host.url=http://192.168.33.10:9000 \
                                    -Dsonar.login=${SONAR_LOGIN} \
                                    -X
                                    """
                                }
                            }
                        }
                    }
                }
            }
        }

        stage('Build Frontend') {
            steps {
                dir('frontend') {
                    echo "🛠️ Construction du frontend..."
                    sh 'npm run build'
                }
            }
        }

        stage('Deploy Docker Image to Nexus') {
            steps {
                script {
                    echo "📤 Déploiement de l'image backend vers Nexus..."
                    dir('backend') {
                        def imageName = "projectmanagement"
                        def imageTag = "${env.BUILD_NUMBER}"
                        def fullImageName = "${imageName}:${imageTag}"
                        
                        sh "docker build -t ${fullImageName} ."
                        docker.withRegistry("http://${NEXUS_URL}", NEXUS_CREDENTIAL_ID) {
                            sh "docker tag ${fullImageName} ${NEXUS_URL}/${imageName}:${imageTag}"
                            sh "docker push ${NEXUS_URL}/${imageName}:${imageTag}"
                        }
                    }
                }
            }
        }
    }

    post {
        success {
            echo '✅ Pipeline exécuté avec succès !'
        }
        failure {
            echo '❌ Le pipeline a échoué. Vérifiez les logs.'
        }
        always {
            echo "📂 Nettoyage de l'espace de travail après exécution..."
            dir('backend') {
                sh 'docker-compose -f docker-compose.yaml down -v --remove-orphans || true'
            }
            sh 'docker system prune -f || true'
            cleanWs()
        }
    }
}